name: Build and Publish AppImage

on:
  push:
    branches: [ main ]
  release:
    types: [ published ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "22"

      - name: Install system dependencies (including FUSE)
        run: |
          sudo apt-get update
          sudo apt-get install -y libfuse2

      - name: Cache Poetry dependencies
        uses: actions/cache@v3
        with:
          path: |
            ~/.cache/pypoetry
            ~/.cache/pip
          key: ${{ runner.os }}-poetry-${{ hashFiles('**/poetry.lock') }}

      - name: Install Poetry
        run: |
          curl -sSL https://install.python-poetry.org | python3 -
          echo "${HOME}/.local/bin" >> $GITHUB_PATH

      - name: Install Dependencies
        run: make install_deps

      - name: Build AppImage
        run: make build_appimage

      - name: Verify AppImage exists
        run: |
          if [ ! -f "Copilot.AppImage" ]; then
            echo "Error: Copilot.AppImage not found!"
            exit 1
          fi

      - name: Upload AppImage Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Copilot.AppImage
          path: Copilot.AppImage

      - name: Publish AppImage to GitHub Releases
        if: ${{ github.event_name == 'release' }}
        uses: ncipollo/release-action@v1
        with:
          artifacts: Copilot.AppImage